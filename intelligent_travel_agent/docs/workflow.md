# Workflow

A request is extracted, validated, and clarified until destination, duration, and budget are complete. Validation concurrently runs policy, guardrail, and weather agents. Weather warnings pause for a user decision. Planning concurrently searches hotels and attractions, then runs itinerary and reviewer stages.
